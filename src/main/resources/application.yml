recipes.quickRecipes.MaxDuration: 30

spring:
#    profiles.active: development # spring.profiles.active possible values are: development, production
    config.additional-location: classpath*:config/

logging.level:
    root: WARN
    org.springframework.web: WARN
    org.hibernate: WARN
    com.tomlouiskeller.recipe: DEBUG


# spring.profiles.active chooses application-{profile}.yml